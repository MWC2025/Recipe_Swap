extends layout

block content
  h2.page-title Recipes tagged: #{tagName}

  .card-grid
    each recipe in recipes
      .card
        h4= recipe.title
        p= recipe.description
        a.btn(href=`/recipes/${recipe.recipe_id}`) View Recipe